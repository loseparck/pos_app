

class PaymentState {
  /*final PaymentMode mode;
  final List<PaymentPart> parts;
  final List<PayableItem> items;
  final int splitCount;

  PaymentState({
    required this.mode,
    required this.parts,
    required this.items,
    this.splitCount = 1,
  });

  double get total =>
      items.fold(0, (s, i) => s + i.item.total);

  double get totalPaid =>
      parts.fold(0, (s, p) => s + p.amount);

  double get remaining => total - totalPaid;*/
}